<x-filament-panels::page>
    <div class="space-y-6">
        <x-filament::section>
            <x-slot name="heading">Currently Active Hero Placements ({{ $activePlacements->count() }} / 10 slots potentially used)</x-slot>
            @if($activePlacements->isNotEmpty())
                <div class="overflow-x-auto">
                    <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400 fi-table">
                        <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                            <tr>
                                <th scope="col" class="px-6 py-3">Business</th>
                                <th scope="col" class="px-6 py-3">Starts</th>
                                <th scope="col" class="px-6 py-3">Expires</th>
                                <th scope="col" class="px-6 py-3">Set By</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach($activePlacements as $placement)
                                <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-600">
                                    <td class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
                                        <a href="{{ \App\Filament\Resources\BusinessResource::getUrl('edit', ['record' => $placement->business_id]) }}" class="hover:underline text-primary-600">
                                            {{ $placement->business->name ?? 'N/A' }}
                                        </a>
                                    </td>
                                    <td class="px-6 py-4">{{ $placement->activated_at->format('M d, Y H:i') }}</td>
                                    <td class="px-6 py-4">{{ $placement->expires_at->format('M d, Y H:i') }}</td>
                                    <td class="px-6 py-4">{{ $placement->admin->name ?? 'N/A' }}</td>
                                </tr>
                            @endforeach
                        </tbody>
                    </table>
                </div>
            @else
                <p>No businesses are currently featured on the hero slider.</p>
            @endif
        </x-filament::section>

        <x-filament::section>
            <x-slot name="heading">Upcoming Hero Placements (Next 30 Days)</x-slot>
             @if($upcomingPlacements->isNotEmpty())
                {{-- Similar table structure as above for $upcomingPlacements --}}
             @else
                <p>No upcoming hero placements scheduled in the next 30 days.</p>
             @endif
        </x-filament::section>
    </div>
</x-filament-panels::page>